# `data/`

| File | Ruolo |
| :--- | :--- |
| `curator-store.example.json` | Seed versionato (vuoto / senza PII). |
| `curator-store.json` | Store runtime locale (**gitignored**). Creato al primo avvio dall’example. |

Non committare `curator-store.json`: può contenere email, roster e override di schede.
